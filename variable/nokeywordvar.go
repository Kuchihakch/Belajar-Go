package main

import "fmt"

func main() {
	// text = "err";
	teks := "Ini Teks Tanpa Var Keyword" // ini hanya di awal aja untuk :=, kalau pakai selanjutnya dianggap redeclare seperti var
	fmt.Print(teks)
}
